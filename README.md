# multi-threaded-library-management-system
A Java-based multi-threaded TCP client–server application designed to simulate a simple Library Management System. The server uses multiple threads to handle simultaneous client connections, while the client communicates with the server to perform operations such as data requests and arithmetic processing.
